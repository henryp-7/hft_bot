# Symbols are Binance spot symbols (lowercase)
SYMBOLS=btcusdt,ethusdt
QUOTE_CCY=USDT

# Starting paper cash (in QUOTE_CCY)
INITIAL_CASH=10000

# Execution mode
USE_TESTNET=false   # true to enable binance testnet for live order placement (otherwise paper)

# Optional slippage in basis points for paper fills
SLIPPAGE_BPS=1

# Risk limits
MAX_NOTIONAL_PER_SYMBOL=5000
MAX_TOTAL_NOTIONAL=10000

# Optional: Binance Spot Testnet API keys (only used if USE_TESTNET=true)
BINANCE_API_KEY=
BINANCE_API_SECRET=
